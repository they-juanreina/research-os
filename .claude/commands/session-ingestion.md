---
description: "Normalize Teams HTML session transcripts into structured JSON and render filled discussion notes from a Jinja2 template."
---

Read `CORE.md`, then read `skills/session-ingestion/SKILL.md` and execute the skill with the following input:

$ARGUMENTS
